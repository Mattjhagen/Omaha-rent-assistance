# Stripe Configuration
# Get these from your Stripe Dashboard: https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key_here
STRIPE_PUBLISHABLE_KEY=pk_test_your_stripe_publishable_key_here

# Stripe Webhook Secret
# Get this from your Stripe Dashboard: https://dashboard.stripe.com/webhooks
# Use the command `stripe listen --forward-to localhost:3000/api/stripe-webhook` to get a webhook secret for local development
STRIPE_WEBHOOK_SECRET=whsec_your_stripe_webhook_secret_here

# Server Configuration
PORT=3000
NODE_ENV=production

# Optional: Database URL (if you want to add database functionality later)
# DATABASE_URL=your_database_connection_string_here
